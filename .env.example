# ─────────────────────────────────────────────
# OptusFrontend – Environment Variables
# Copy this file to .env and fill in the values.
# ─────────────────────────────────────────────

# Privy (auth)
VITE_PRIVY_APP_ID=

# Supabase
# Get these from: Supabase Dashboard → Project Settings → API
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
# Use the "anon / public" key. Never expose the service_role key in the frontend.
VITE_SUPABASE_ANON_KEY=

# Google OAuth
VITE_GOOGLE_OAUTH_CLIENT_ID=
VITE_GOOGLE_OAUTH_CLIENT_SECRET=

# WalletConnect (RainbowKit)
VITE_WALLETCONNECT_PROJECT_ID=

# Backend (X402 payment agent)
VITE_AGENT_BACKEND_URL=

# Dashboard identity
# The company UUID to scope all Supabase queries (multi-tenant filter)
VITE_DEMO_COMPANY_ID=
# Display name shown in the dashboard header
VITE_DEMO_COMPANY_NAME=

# ─────────────────────────────────────────────
# Supabase tables used by the dashboard
# ─────────────────────────────────────────────
# products          – id, company_id, sku, name, price, stock_quantity, is_available
# supply_items      – id, company_id, product_id
# inventory_policies– supply_item_id, reorder_point
# orders            – id, company_id, status, total_amount, created_at
# order_items       – order_id, product_id, quantity
# payments          – order_id, status, amount, currency, verified_at, provider_ref
# stock_movements   – id, company_id, movement_type (IN/OUT), quantity,
#                     ref_order_id, ref_purchase_order_id, created_at
# procurement_requests – id, company_id, status, requested_qty, created_at
# purchase_orders   – id, company_id, procurement_request_id, po_number,
#                     total_amount, status, created_at
# supplier_payments – id, company_id, purchase_order_id, status, amount,
#                     currency, tx_hash, created_at
# suppliers         – id, name  (joined from purchase_orders)
